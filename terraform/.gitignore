# Local Terraform directories
.terraform/
*.tfstate
*.tfstate.*.backup
*.tfvars

# Crash log files
crash.log
crash.*.log

# Exclude all .tfstate files in subdirectories (e.g., for Terraform Cloud)
**/*.tfstate
**/*.tfstate.backup

# Terraform lock file
.terraform.lock.hcl

# Ignore override files
*.override.tf
*.override.tf.json
*_override.tf
*_override.tf.json

# Secrets files - ensure these are handled by a proper secrets management solution if not versioned
# secrets.sops.yaml # Example, if you don't want to commit it, though sops encrypts it.
# kubekey.pub # Example, if it's sensitive and not meant to be in repo
# kubekey # Example private key

# IDE / Editor specific files
.vscode/
.idea/
*.swp
