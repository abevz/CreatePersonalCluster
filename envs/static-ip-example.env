# Example configuration for static IP addresses
# Copy this to your workspace.env file and uncomment the lines to enable static IPs

# Static IP configuration
# STATIC_IP_BASE="10.10.10."  # Base IP address (note the trailing dot)
# STATIC_IP_GATEWAY="10.10.10.1"  # Gateway IP address
# STATIC_IP_START="100"  # Starting IP offset (e.g., 100 = .100, .101, .102, etc.)

# Example configuration:
# With STATIC_IP_BASE="10.10.10." and STATIC_IP_START="100":
# - controlplane node will get 10.10.10.100
# - worker1 node will get 10.10.10.101  
# - worker2 node will get 10.10.10.102
# - additional workers continue incrementally

# If these variables are not set or empty, nodes will use DHCP
